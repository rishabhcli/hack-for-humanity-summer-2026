# Work journal

This file is append-only. Each entry records what changed, the commands that were actually run, the evidence produced, and the next item selected by `GOAL.md` §10.1.

## 2026-08-09 23:27 PDT — Tier 0 executable contract and owned local runtime

### Behaviour delivered

- Established a repository-local, exact-pinned npm/TypeScript/Vite/Vitest/Playwright toolchain instead of inheriting the unrelated package boundary in the user's home directory.
- Implemented the complete allocated local topology on HTTPS `127.0.0.1:4180-4183`, backed by committed `ports.env` and an ignored `.dev/` runtime tree.
- Implemented atomic PID records, run-scoped readiness authentication, certificate pinning, lifecycle serialization, listener/PID/cwd/command validation, exact-PID shutdown, bounded readiness polling, and fail-closed handling of foreign or unverifiable processes.
- Added a truthful local PWA shell that exposes no measurement result and says the repository is not yet in production; it performs only a local runtime-readiness check.
- Added strict product/tool/script type-checking, lint and import-boundary enforcement, unit/property coverage, real lifecycle integration tests, Playwright browser checks, dependency evidence generation, a canonical `verify-all`, and SHA-pinned GitHub Actions configuration.
- Recorded the toolchain and port-conflict decisions in ADRs, the dependency assessment in `docs/DEPENDENCIES.md`, assumptions and support boundaries in their required registers, and external blockers separately.

### Commands run and observed results

- `npm ci` — passed from the repository-local lockfile on the development host.
- `npm audit --audit-level=low` — passed with zero reported vulnerabilities after rejecting the vulnerable attempted `esbuild@0.27.7` quarantine and retaining `esbuild@0.28.2`.
- `npm run test:unit` — passed 23 tests, including a deterministic `fast-check` port-parser property with 250 cases and seed `20260809`.
- `npm run test-integration` — passed the `stale-pid`, `forged-pid`, `foreign-listener`, `health-timeout`, `owned-down`, and `restore` scenarios; the foreign listener survived and teardown remained PID-scoped.
- `npm run dev:preflight && npm run dev:up && npm run dev:health` — passed; readiness checked service identity/body, ownership, listener binding, certificate, configuration digest, and built-artifact digest rather than accepting a TCP socket alone.
- `npm run test:e2e:runner` — the readiness and no-third-party-request checks passed, but the new offline PWA check exposed a real failure: Chromium rejected the repository-local certificate when the service worker fetched `/sw.js` even though normal page navigation used Playwright's `ignoreHTTPSErrors` setting.
- `npm run evidence:verify` — reached the same service-worker failure after format, lint, dependency, audit, typecheck, unit, build, integration, preflight, up, and health checks passed. It correctly wrote a failed result rather than a false-green artifact.
- A direct Playwright diagnostic reproduced `SecurityError: Failed to register a ServiceWorker ... An SSL certificate error occurred when fetching the script.` and confirmed that no registration existed.

### Evidence produced

- `evidence/tier-0/dependencies.json`, regenerated by `npm run evidence:dependencies`, records the exact locked dependency set and review metadata.
- `evidence/tier-0/verify-all-local.log`, regenerated by `npm run evidence:verify`, currently and honestly ends in failure at the service-worker/offline check. It must not be used as passing Tier 0 evidence until regenerated successfully.
- `.dev/logs/`, `.dev/pids/`, and `.dev/tls/` contain ignored operational artifacts for the live local cohort; none is commit evidence or a production claim.

### What is now true that was not true before

The repository can start, identify, health-check, and stop only its own four allocated services, and its automated failure tests exercise the most dangerous foreign-process and stale-ownership paths. It can also reject unsupported measurement rather than imply functionality that has not been built. Tier 0 is not yet exited: the offline browser check is red, no clean committed checkout has run, and no GitHub CI run exists.

### Risks, migration, rollback, and blockers

- The development certificate is intentionally repository-local rather than publicly trusted. Playwright must model explicit test trust without weakening `dev:health` certificate pinning or shipped browser behavior.
- There is no persistent product data, so this slice has no data migration. Rollback is removal of the new executable foundation plus `npm run dev:down`; broad process termination is never permitted.
- No external blocker applies to the immediate work. All production and measurement gates remain unverified.

### Next item selected by §10.1

Fix the failing PWA service-worker/offline browser gate without bypassing the real registration path, rerun the targeted E2E suite, then regenerate the full local verification log. The standing `dev:health` gate remains first and must be rechecked before downstream work.

## 2026-08-10 02:07 PDT — Documentation correction: reproducibility, layout, ports, and dependency evidence

### Correction to the preceding Tier 0 journal entry

- The canonical clean-checkout sequence is exactly `npm run bootstrap` followed by `npm run verify-all`. The latter already owns preflight, startup, health, and browser verification; listing those lifecycle commands as additional clean-checkout prerequisites was documentation drift.
- The `4180-4183` mapping is fixed committed configuration, not a runtime relocation surface. Any approved change must synchronize `ports.env`, lifecycle and Playwright configuration/tests, `ASSUMPTIONS.md`, ADR-0002, and clean-checkout evidence.
- Planned domain paths are policy-reserved ownership names. Their absence is truthful; no physical directory is created until real production code, tests, and documentation exist there.
- The earlier entry remains evidence of the commands observed at that time, including its failed full verification. These corrections do not convert that failure into a pass, establish a clean-checkout result, establish CI, or satisfy a release gate.

### Documentation and evidence checks run

- `npm audit --audit-level=low --json` — exited 0 at `2026-08-10T08:59:49Z` and reported zero vulnerabilities at every severity across the locked 503-package graph.
- Exact-version queries to `registry.npmjs.org` for all 12 direct development dependencies — each returned HTTP 200 with the license recorded in `docs/DEPENDENCIES.md` and no `deprecated` field.
- The dependency register now reports those dated observations per direct version and explicitly limits “no audit path” to npm's current locked-graph advisory result; it does not claim an independent package audit or absence of unknown vulnerabilities.

### Next item selected by §10.1

+Re-establish a green local verification run after the current lifecycle and boundary hardening is internally reviewed, then obtain clean-checkout and CI evidence without describing any production release gate as passed.

### Typographical clarification

The leading `+` in the preceding “Next item” sentence is a transcription mark, not a status or claim. The selected item is: re-establish a green local verification run after the current lifecycle and boundary hardening is internally reviewed, then obtain clean-checkout and CI evidence without describing any production release gate as passed.

### Operational-path clarification

The ignored certificate artifacts named as `.dev/tls/` in the 2026-08-09 entry are actually stored under `.dev/certs/`. The earlier path was documentation drift; no `.dev/tls/` contract exists.

The current Vitest percentage covers only the explicitly configured `src/**/*.ts` product-code scope (excluding the browser composition root). Lifecycle, boundary, evidence, and other Tier 0 scripts are type-checked and exercised by unit/integration tests, but are not part of that percentage; it must not be described as repository-wide or Tier 0-wide coverage.

## 2026-08-10 06:52 PDT — Tier 0 adversarial lifecycle, build, and private-path hardening

### Behaviour and boundaries changed

- Replaced the loose PID-record lifecycle with exact schema 2 records bound to service, configured port, repository, canonical command, process start, cwd, run ID, and a per-run ownership token. Shutdown now revalidates the exact sole IPv4 loopback listener immediately before each signal and refuses ambiguity.
- Added lock-owner fingerprints and tokens, blocking lock waits instead of CPU busy-spins, reader serialization, sanitized child environments, post-spawn rollback, and symlink-refusing repository reads, logs, digests, build inputs, PID records, and certificate generation.
- Centralized desired service artifact digests. `dev:health` now rejects a stale record/process pair even when its HMAC response agrees with that stale record.
- Added deterministic lockfile-only CycloneDX evidence representing all 503 lock entries as 498 component identities, including Darwin and Linux optional binaries. Host timestamps, serials, cwd names, and paths are removed and validation is byte-for-byte.
- Added a two-build path-and-SHA reproducibility gate and removed nondeterministic, host-path-bearing Workbox service-worker source maps. Build execution now rejects `.env*`, sanitizes inherited environment, checks inputs/output before Vite, and records ignored build provenance so repeated owned startup does not rerun an unchanged verified build.
- Added an earliest Vite middleware and filesystem deny/watch-ignore rules for every `/.dev/` path. The regression uses Node HTTPS outside Playwright tracing and compares only a leak boolean so a failing assertion cannot print a readiness secret.
- Added key/certificate public-key matching and temporary-directory publication so a partial or mismatched local TLS pair is regenerated rather than accepted.

### Failures found and commands actually observed

- A hardened `npm run test-integration` run failed after about fifteen minutes with `INTEGRATION_HEALTHY_SERVICE_NOT_RETAINED service=preview`. Inspection found `dist/sw.js.map` contained a random `/private/var/...` source path and absolute repository imports; the map made every build digest different. The retention assertion was kept, Workbox source maps were disabled, and a direct two-build run then produced identical output: 8 files, 36,823 bytes, manifest SHA-256 `4750376cedb8e16a6a634bdc2afb131c62bface60490f5e8d8918bc1a2959f13`.
- Before the explicit middleware existed, safe read-only probes confirmed that the development PWA served `.dev/pids/pwa.json` and `.dev/logs/pwa.log`. After the filesystem deny was added, real files returned 403 without leaked fields, but a nonexistent `.dev/tmp/openssl.cnf` still fell through to the SPA with 200. That false-negative caused the path-segment middleware and encoded-path regressions; the final E2E rerun is still required.
- `npm run dev:health` deliberately failed with `DEV_HEALTH_ARTIFACT_STALE service=pwa` after control-plane edits. `npm run dev:up` then rebuilt/restarted all four owned services, and the next health run returned ready for 4180-4183. Further hardening edits after that observation make a fresh startup/health run necessary again.
- Focused strict formatting, ESLint, tool TypeScript, checked-JavaScript TypeScript, and lifecycle unit runs passed after the symlink/listener changes. The latest focused lifecycle run passed 23 tests; an earlier full unit run passed 57 tests. Neither is substituted for the pending full canonical run.
- The prior failed `evidence/tier-0/verify-all-local.log` was removed. It will be regenerated only by `npm run evidence:verify` starting from a clean committed tree and will bind the verified commit/tree; its absence is not passing evidence.

### Remaining truth and next §10.1 item

Tier 0 is still open. The current working tree is uncommitted, no clean checkout or GitHub Actions run exists, per-direct dependency maintenance/security-history evidence is being made regenerable, the new TLS/private-path/build/lifecycle regressions need one coherent full rerun, and the coverage percentage remains product-source-scoped. The selected next item is to finish that dependency evidence, re-establish `dev:health`, run the hardened lifecycle and browser suites, then obtain clean-commit and CI evidence without claiming any production release gate.
